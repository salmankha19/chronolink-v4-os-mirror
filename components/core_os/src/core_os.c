#include "core_os.h"
#include "esp_log.h"

static const char *TAG = "core_os";

void core_os_init(void) {
  ESP_LOGI(TAG, "core_os init");
}

void core_os_start(void) {
  ESP_LOGI(TAG, "core_os start");
}
