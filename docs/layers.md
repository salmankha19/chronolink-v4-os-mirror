# ChronoLink V4 Layers

- PDL Board Layer: board profile, pin map, partition notes.
- PIL / HAL Layer: stable C ABI and MCU-specific drivers.
- Service / Engine Layer: pure logic (state, scheduler, prayer engine).
- Application Layer: UI, web config, display cycle.


