[1mdiff --git a/components/state_manager/CMakeLists.txt b/components/state_manager/CMakeLists.txt[m
[1mindex 0e62c7d..bd07fd1 100644[m
[1m--- a/components/state_manager/CMakeLists.txt[m
[1m+++ b/components/state_manager/CMakeLists.txt[m
[36m@@ -1,5 +1,5 @@[m
 ﻿idf_component_register([m
     SRCS "src/state_manager.c"[m
[31m-    INCLUDE_DIRS include[m
[32m+[m[32m    INCLUDE_DIRS "include"[m
     REQUIRES freertos[m
[31m-)[m
\ No newline at end of file[m
[32m+[m[32m)[m
[1mdiff --git a/components/state_manager/include/state_manager.h b/components/state_manager/include/state_manager.h[m
[1mnew file mode 100644[m
[1mindex 0000000..013f75d[m
[1m--- /dev/null[m
[1m+++ b/components/state_manager/include/state_manager.h[m
[36m@@ -0,0 +1,19 @@[m
[32m+[m[32m#pragma once[m
[32m+[m
[32m+[m[32m#include "freertos/FreeRTOS.h"[m
[32m+[m[32m#include "freertos/queue.h"[m
[32m+[m[32m#include <stdint.h>[m
[32m+[m
[32m+[m[32m/* Forward declaration of the UIState struct tag only (no typedef).[m
[32m+[m[32m   This avoids conflicting with the canonical typedef in ui_state.h. */[m
[32m+[m[32mstruct UIState;[m
[32m+[m
[32m+[m[32mtypedef struct {[m
[32m+[m[32m    uint32_t id;[m
[32m+[m[32m    uint32_t value;[m
[32m+[m[32m} os_state_msg_t;[m
[32m+[m
[32m+[m[32m/* Initialize the state manager with an existing queue handle.[m
[32m+[m[32m   The kernel (core_os) creates the queue and passes the handle here.[m
[32m+[m[32m   Idempotent and safe to call multiple times. */[m
[32m+[m[32mvoid state_manager_init(QueueHandle_t queue);[m
[1mdiff --git a/components/state_manager/src/state_manager.c b/components/state_manager/src/state_manager.c[m
[1mindex 54c2e5b..c0ff8eb 100644[m
[1m--- a/components/state_manager/src/state_manager.c[m
[1m+++ b/components/state_manager/src/state_manager.c[m
[36m@@ -1,15 +1,98 @@[m
[31m-﻿#include "ui_state.h"[m
[32m+[m[32m﻿#include "ui_state.h"           /* canonical declarations for state_get_snapshot and state_publish_update */[m
[32m+[m[32m#include "state_manager.h"[m
 #include "esp_log.h"[m
[32m+[m[32m#include "freertos/FreeRTOS.h"[m
[32m+[m[32m#include "freertos/semphr.h"[m
[32m+[m[32m#include "freertos/queue.h"[m
[32m+[m[32m#include <string.h>[m
 [m
 static const char *TAG = "state_manager";[m
 [m
[32m+[m[32m/* Internal UI state (kept private) */[m
 static UIState current = {0, "00:00", 50};[m
 [m
[31m-const UIState *state_get_snapshot(void) {[m
[31m-  return &current;[m
[32m+[m[32m/* Synchronization primitives */[m
[32m+[m[32mstatic SemaphoreHandle_t state_lock = NULL;[m
[32m+[m
[32m+[m[32m/* Optional queue handle provided by core_os during initialization */[m
[32m+[m[32mstatic QueueHandle_t g_state_queue = NULL;[m
[32m+[m
[32m+[m[32m/* Ensure the mutex exists; idempotent */[m
[32m+[m[32mstatic void state_manager_init_once(void)[m
[32m+[m[32m{[m
[32m+[m[32m    if (state_lock == NULL) {[m
[32m+[m[32m        state_lock = xSemaphoreCreateMutex();[m
[32m+[m[32m        if (state_lock == NULL) {[m
[32m+[m[32m            ESP_LOGE(TAG, "Failed to create state mutex");[m
[32m+[m[32m        } else {[m
[32m+[m[32m            ESP_LOGI(TAG, "State Manager mutex created");[m
[32m+[m[32m        }[m
[32m+[m[32m    }[m
[32m+[m[32m}[m
[32m+[m
[32m+[m[32m/* Public: initialize the state manager with an existing queue handle.[m
[32m+[m[32m   Records the queue handle and ensures the mutex exists. */[m
[32m+[m[32mvoid state_manager_init(QueueHandle_t queue)[m
[32m+[m[32m{[m
[32m+[m[32m    if (g_state_queue != NULL) {[m
[32m+[m[32m        ESP_LOGI(TAG, "state_manager_init: already initialized (queue=%p)", (void*)g_state_queue);[m
[32m+[m[32m        return;[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    if (queue == NULL) {[m
[32m+[m[32m        ESP_LOGE(TAG, "state_manager_init: provided queue is NULL");[m
[32m+[m[32m        /* still ensure mutex exists so other APIs remain usable */[m
[32m+[m[32m        state_manager_init_once();[m
[32m+[m[32m        return;[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    g_state_queue = queue;[m
[32m+[m[32m    state_manager_init_once();[m
[32m+[m
[32m+[m[32m    ESP_LOGI(TAG, "state_manager_init: initialized with queue=%p", (void*)g_state_queue);[m
 }[m
 [m
[31m-void state_publish_update(const UIState *s) {[m
[31m-  (void)s;[m
[31m-  ESP_LOGI(TAG, "state update");[m
[32m+[m[32m/* Return a pointer to the current UIState snapshot (read-only).[m
[32m+[m[32m   Signature must match the declaration in ui_state.h. */[m
[32m+[m[32mconst UIState *state_get_snapshot(void)[m
[32m+[m[32m{[m
[32m+[m[32m    state_manager_init_once();[m
[32m+[m
[32m+[m[32m    if (state_lock && xSemaphoreTake(state_lock, pdMS_TO_TICKS(10)) == pdTRUE) {[m
[32m+[m[32m        /* We intentionally return pointer to internal state; caller must treat as read-only. */[m
[32m+[m[32m        xSemaphoreGive(state_lock);[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    return &current;[m
[32m+[m[32m}[m
[32m+[m
[32m+[m[32m/* Update the internal UI state in a thread-safe manner and optionally post to queue.[m
[32m+[m[32m   Signature must match the declaration in ui_state.h. */[m
[32m+[m[32mvoid state_publish_update(const UIState *s)[m
[32m+[m[32m{[m
[32m+[m[32m    if (!s) {[m
[32m+[m[32m        return;[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    state_manager_init_once();[m
[32m+[m
[32m+[m[32m    if (state_lock && xSemaphoreTake(state_lock, pdMS_TO_TICKS(10)) == pdTRUE) {[m
[32m+[m[32m        current.unix_ts    = s->unix_ts;[m
[32m+[m[32m        current.brightness = s->brightness;[m
[32m+[m[32m        strncpy(current.time_str, s->time_str, sizeof(current.time_str) - 1);[m
[32m+[m[32m        current.time_str[sizeof(current.time_str) - 1] = '\0';[m
[32m+[m[32m        xSemaphoreGive(state_lock);[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    ESP_LOGI(TAG, "UI state updated: ts=%lu time=%s brightness=%u",[m
[32m+[m[32m             (unsigned long)current.unix_ts,[m
[32m+[m[32m             current.time_str,[m
[32m+[m[32m             current.brightness);[m
[32m+[m
[32m+[m[32m    /* If a state queue was provided at init, optionally post a lightweight notification.[m
[32m+[m[32m       This is non-blocking and will not fail the update if the queue is full. */[m
[32m+[m[32m    if (g_state_queue) {[m
[32m+[m[32m        os_state_msg_t msg = { .id = 0, .value = 0 }; /* populate as needed */[m
[32m+[m[32m        (void)xQueueSend(g_state_queue, &msg, 0);[m
[32m+[m[32m    }[m
 }[m
